name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: build & test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Odin
        uses: laytan/setup-odin@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          release: nightly

      - name: odin version
        run: odin version

      - name: Bootstrap build (build.sh)
        if: runner.os != 'Windows'
        run: ./build.sh

      - name: Bootstrap build (build.ps1)
        if: runner.os == 'Windows'
        run: ./build.ps1

      - name: Doctor
        if: runner.os != 'Windows'
        run: ./bin/loki doctor

      - name: Doctor (Windows)
        if: runner.os == 'Windows'
        run: .\bin\loki.exe doctor

      - name: Build all apps
        if: runner.os != 'Windows'
        run: ./bin/loki build

      - name: Build all apps (Windows)
        if: runner.os == 'Windows'
        run: .\bin\loki.exe build

      - name: Test all local packages
        if: runner.os != 'Windows'
        run: ./bin/loki test

      - name: Test all local packages (Windows)
        if: runner.os == 'Windows'
        run: .\bin\loki.exe test
